/*import nc from 'next/connect';

const handler = nc();

handler.get(async (req, res) => {
  const categories = ['salon', 'exterieur', 'deco'];
  res.send(categories);
});

export default handler;*/ // Ici j'ai commencé mais ça prenait beaucoup de temps.
